// playground (ctrl j)

#import 'library/common.js'

var artboards = Document.artboards()

_.each(artboards, function(artboard){
  _.each(artboard.groups(), function(group){
    console.log(group.coords.x)
  })
})
